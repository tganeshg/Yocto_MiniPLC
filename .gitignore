# This repo only tracks the bootstrap (bin/) and the product layer
# (meta-miniplc/). Everything else is fetched by `bin/setup_yocto.sh`
# and stays out of git.

# Upstream layers — pinned and cloned by setup_yocto.sh
/poky/
/meta-openembedded/
/meta-raspberrypi/

# BitBake build tree — regenerated by oe-init-build-env
/build/

# Editor / OS noise
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/

# Local override anyone might drop in
local.conf.local
*.local
