# MiniHMI network configuration — managed by the HMI app.
# Edit via the touchscreen UI (Settings -> NETWORK tile) or run
# /usr/bin/mdcu-net-apply by hand after editing.

mode=dhcp
iface=eth0
address=
netmask=
gateway=
dns1=
dns2=
